

//Receive a boolean value and convert it in a string

function boolean_string(val_bool: boolean): string {
    //Cast to string
   //return String(val_bool);

   //Convert the boolean into a string
   return val_bool.toString();
   
}

